name: GitGuardian Scan

on:
  pull_request:
    branches:
      - main
      - nonprod
  push:
    branches:
      - main
      - nonprod
permissions: write-all

jobs:
  scanning:
    name: GitGuardian scan
    env:
      ggshield_access_token: ${{ secrets.ggshield_access_token }}
    uses: reminder-bot-microservices/.github/.github/workflows/scan_leaks.yaml@main
    secrets: inherit
